# Copy to .env.deploy and fill in. NEVER commit .env.deploy.
# Find these in hPanel -> Advanced -> SSH Access.

# Either a password...
HOSTINGER_SSH_PASSWORD=

# ...or an SSH key (preferred).
# HOSTINGER_SSH_KEY_PATH=~/.ssh/id_ed25519
# HOSTINGER_SSH_PASSPHRASE=
