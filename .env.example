# فهد — ElevenLabs Conversational AI agent ID (public; safe to ship in a
# static build). Without it the فهد launcher is not rendered at all.
# Create the agent in ElevenLabs, then paste its ID here and rebuild.
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=
